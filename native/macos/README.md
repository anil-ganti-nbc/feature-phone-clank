# Feature Phone Clank macOS field test

The thin launcher runs the canonical core and local loopback dashboard using `FEATURE_PHONE_CLANK_DATA_DIR`, defaulting to `~/Library/Application Support/Feature Phone Clank`. It never bundles production state or secrets.
