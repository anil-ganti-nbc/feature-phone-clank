# Windows symmetry

Windows packaging must use the same canonical core and loopback UI, honouring `FEATURE_PHONE_CLANK_DATA_DIR` and defaulting to `%LOCALAPPDATA%\Feature Phone Clank`. No Windows executable is introduced here.
