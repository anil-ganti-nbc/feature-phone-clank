.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
data/*.db
data/*.db-*
data/*.log
data/*.lock
logs/

# Local AI-tool config, not project content
.claude/
logs/

# Compose env substitutions live next to the compose file on the host
# (may carry the Discord webhook URL). Never commit.
.env
